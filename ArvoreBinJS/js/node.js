class Node{                                                 // criando a classe
    constructor(element){                                   // criando o construtor
        this.content = element;                             // passando valor para variavel ( conteudo recebe o elemento)
        this.left = null                                    // passando valor para variavel ( esquerda recebe vazio)
        this.right = null;                                  // passando valor para variavel ( direita recebe valor)
    }
}
