class Node{ //criando a classe.
    constructor(element){ //criando um construtor da classe
        this.content = element; // criando uma variavel que recebe o elemento
        this.next = null; // criando uma variavel para proximo, receberá nada porém quando usado irá receber o próximo
    }
}